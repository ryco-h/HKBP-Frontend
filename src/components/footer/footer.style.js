import { makeStyles } from "@mui/styles";

export const useFooterStyles = makeStyles({
    
    root: {

    },
    container: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        margin: '20px'
    }
})